# =============================================================================
# Shared ServiceAccount for all services (Dev)
# =============================================================================
# PROJECT_ID is replaced by CI/CD
apiVersion: v1
kind: ServiceAccount
metadata:
  name: app
  namespace: app
  annotations:
    iam.gke.io/gcp-service-account: telecom-app-app@PROJECT_ID.iam.gserviceaccount.com
